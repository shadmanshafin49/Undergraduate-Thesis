# Base-paper comparison — ULB

_Auto-generated by `experiments/basepaper_comparison.py`._

**Provenance.** Prabanand & Thanabal (2025) compare their DB-BOA-ADTCN against EfficientNet, ResNet, DenseNet and DTCN (classifiers) and MBO, WSA, DBOA and BOA (optimisers), in MATLAB, on their own data. None of their published numbers appear below. Every baseline is re-implemented in this repository and re-run on our data under our protocol, so the whole table is reproducible from `results/`.

**Protocol.** first 80 % by Time -> train, last 20 % -> test; time-ordered global windows (ULB has no customer IDs). 227,845 training rows (417 fraud), 56,962 test rows (75 fraud), 30 features, window 10, 5 epochs, 32 filters, seeds [42, 7, 123].

## Optimiser comparison (base paper Tables 2-3 equivalent)

All five search the same space (filters 8-64, steps/epoch 50-250) with the same population (10) and iteration count (8), minimising the same fitness (Obf2 = 2*MCC + Spec + Pre + NPV (bounded form)), tuning ADTCN (ours).

> ⚠ **Surrogate protocol: `legacy` — the pre-OBJ-13 objective.** Fitness redraws its 70/30 split and its torch seed on every call, so it is a *random function of its input*: one fixed configuration re-evaluated 25× on ULB spans Obf2 3.4497–5.0000 and touches the 5.0000 ceiling once **with no search involved**. `batch_size = max(32, n_train // spe)` also pins to 32 across the whole searched range, so the steps/epoch axis is dead and every steps/epoch optimum below is arbitrary. **Read the `Obf2 best` column as a best-of-N noise statistic, not as a ranking.** See `OBJECTIVE_noise_audit.md`.

| Optimiser | Obf2 best | Obf2 mean ± std | Test MCC | Objective evals | Search s | Filters chosen |
|---|---|---|---|---|---|---|
| MBO-ADTCN | 5.0000 | 4.8009 ± 0.3448 | +0.2815 ± 0.048 | 90 | 249 | [55, 17, 17] |
| WSA-ADTCN | 5.0000 | 4.8947 ± 0.1824 | +0.2243 ± 0.046 | 169 | 459 | [18, 35, 49] |
| DBOA-ADTCN | 5.0000 | 4.8987 ± 0.1754 | +0.2444 ± 0.021 | 166 | 443 | [13, 13, 18] |
| BOA-ADTCN | 5.0000 | 4.8594 ± 0.1608 | +0.2554 ± 0.080 | 170 | 440 | [12, 19, 48] |
| DB-BOA-ADTCN | 5.0000 | 4.8947 ± 0.1824 | +0.2351 ± 0.024 | 139 | 362 | [41, 38, 18] |

### Three caveats that decide how this table may be read

**1. Equal population and iterations is NOT equal objective evaluations.** Each algorithm calls the fitness a different number of times per iteration by construction: MBO 90, WSA 169, DBOA 166, BOA 170, DB-BOA 139. That is a 1.9x range. The budget is matched the way this literature matches it — population and iteration count — not by evaluation count, and the counts are printed above so the reader can judge rather than assume.

**2. The test-MCC differences are inside the noise.** The spread across all five optimisers is 0.0572, while the largest per-optimiser spread across seeds is 0.0802 — larger than the effect being claimed. On the metric that actually matters, **these optimisers are not distinguishable from one another.** DBOA-ADTCN has the best mean search fitness and MBO-ADTCN the best test MCC, and on this evidence neither lead is meaningful.

**3. Search wall-clock is confounded, not an efficiency measure.** The surrogate's cost scales with the filter count a candidate proposes, so an optimiser that explores wide-filter regions pays more per evaluation regardless of its own efficiency. Seconds-per-evaluation therefore varies several-fold across the table for reasons that have nothing to do with optimiser quality. It is reported for transparency and should not be quoted as a speed comparison — which is precisely what the base paper's Table 5 does.
